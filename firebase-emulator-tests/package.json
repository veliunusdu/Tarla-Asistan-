{
  "name": "tarla-asistani-firestore-emulator-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test rules.test.mjs",
    "test:emulator": "firebase emulators:exec --config ../firebase.json --project tarla-asistani-local --only firestore \"node --test rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
